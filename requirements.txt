# Runtime deps for the t1t2 package (model/loss/data) + the vendored generator.
#
# Torch is deliberately NOT listed here. The cluster environment already provides its CUDA build;
# listing it here could silently replace that build with a CPU-only one.
#
# numpy is pinned exactly: the generator's reproducibility is a claim about a *recorded*
# environment — NumPy does not guarantee Generator bit streams across versions, and the manifest
# records which one made each dataset.
numpy==1.26.4
pandas>=2.0
pyarrow>=12.0
scipy>=1.10
pyyaml>=6.0
matplotlib>=3.7
pytest>=7.0
# the data-audit gate runs headless on the cluster
nbconvert>=7.0
ipykernel>=6.0
